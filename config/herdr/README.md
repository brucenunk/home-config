# Herdr configuration

The reusable Home Manager `herdr` capability installs Herdr's configuration,
themes, and Pi integration. It does not synchronize local or remote development
worktrees with Herdr Spaces.